<main class="container">
  <%= link_to sanitize("&larr; Back to quotes"), tasks_path %>
  <div class="header">
    <h1>New task</h1>
  </div>
<%= turbo_frame_tag @task do %>
  <%= render "form", task: @task %>
<% end %>
</main>

<%# turbo_frame_tag "new_task"
turbo_frame_tag Task.new
turbo_frame_tag @task %>